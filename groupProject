//Include all necessary libraries
#include <stdio.h>
#define SIZE 30 
#define TAX .1025

//Drive the program
int main()
{
	//Declare local variables
	int seats[SIZE] = {};
	int customerSeats[SIZE] = {};
	float total = 0;
	int counter = 0;
	int choice;
	float bagsCost;
	//char response;
//	int flag;
	float salesTax = 0;
	int price;

		printf("Please type 1 for \"first class\"\n");
		printf("Please type 2 for \"business class\"\n");
		printf("Please type 3 for \"economy class\"\n");
		printf("Please type 0 to \"exit\"\n");
		scanf("%d", &choice);
printf("Please enter the class you would like to sit in");
scanf("%d", &choice);

while (choice != 0) {
switch (choice) {
case 1:
printf("You have picked first class\n");

price = 325.00;
for (i = 0; i < seats[i]; ++i){
customerSeats[x];
}
printf("Please enter the amount of bags you will have with you");
scanf("%d", &bag);
if (bag > 1) {
bagsCost = (bag * 5.75) + price;
}
purchase = baggage + price ;
salesTax = TAX * purchase ;
total = salesTax + purchase;


counter += counter ;
break;

case 2:
printf("You have picked business class\n");

tickprice = 275.00;
for (i = 0; i < seats[i]; ++i){
customerSeats[x];
}
printf("Please enter the amount of bags you will have with you");
scanf("%d", &bag);
if (bag > 1) {
bagsCost = (bag * 5.75) + price;
}
purchase = baggage + price ;
salesTax = TAX * purchase ;
total = salesTax + purchase;
counter += counter ;
break;

case 3:
printf("You have picked economy class\n");

price = 200.00;
for (i = 0; i < seats[i]; ++i){
customerSeats[x];
}
printf("Please enter the amount of bags you will have with you");
scanf("%d", &bag);
if (bag > 1) {
bagsCost = (bag * 5.75) + tickprice;
}
purchase = baggage + price ;
salesTax = TAX * purchase ;
total = salesTax + purchase;
counter += counter ;
break;
}\\ end of switch 

printf("Please enter the class you would like to sit in");
scanf("%d", &choice);
}// end of function
